---
meta:
  title: Entity | Custom Entity | SaasRock
  description: Entity model life cycle, properties and model
headers:
  Cache-Control: no-cache
---

import DocBreadcrumb from "~/components/ui/docs/DocBreadcrumb";

<DocBreadcrumb
  items={[
    {
      title: "Custom Entity Models",
      routePath: "/docs/core-models/custom-entity",
    },
    {
      title: "Entity",
      routePath: "/docs/core-models/custom-entity/entity",
    },
  ]}
/>

# Entity

- Customizable application entities.
- Autogenerated view at **_/app/:tenant/:entity-slug_**.
- Autogenerated form at **_/app/:tenant/:entity-slug/new_**.
- Autogenerated view and form at **_/app/:tenant/:entity-slug/:id_**.

## Life Cycle

import DocLifeCycle from "~/components/ui/docs/DocLifeCycle";

<DocLifeCycle
  items={[
    {
      route: "/admin/entities",
      c: false,
      r: true,
      u: false,
      d: false,
    },
    {
      route: "/admin/entities/new",
      c: true,
      r: false,
      u: false,
      d: false,
    },
    {
      route: "/admin/entities/:entity-slug",
      c: false,
      r: true,
      u: true,
      d: true,
    },
  ]}
/>

## Properties

import DocPropertiesTable, {
  ModelPropertyType,
} from "~/components/ui/docs/DocPropertiesTable";

<DocPropertiesTable
  items={[
    {
      name: "name",
      title: "Name",
      type: ModelPropertyType.String,
      description:
        "Unique internal identifier, e.g. employee.",
      required: true,
    },
    {
      name: "slug",
      title: "Slug",
      type: ModelPropertyType.String,
      description:
        "Unique application URL, e.g. employees would show at /app/:tenant/employees",
      required: true,
    },
    {
      name: "title",
      title: "Title",
      type: ModelPropertyType.String,
      description:
        "Can be a i18n key for multi-language support. e.g. Employee or models.employee.object.",
      required: true,
    },
    {
      name: "titlePlural",
      title: "Plural Title",
      type: ModelPropertyType.String,
      description:
        "Can be a i18n key for multi-language support. e.g. Employees or models.employee.plural.",
      required: true,
    },
    {
      name: "order",
      title: "Order",
      type: ModelPropertyType.Int,
      description:
        "Order of display at the /app sidebar menu.",
      required: true,
    },
    {
      name: "prefix",
      title: "Prefix",
      type: ModelPropertyType.String,
      description:
        "Used for folios, e.g. EMP-0001, EMP-0002...",
      required: true,
    },
    {
      name: "isFeature",
      title: "Is Feature",
      type: ModelPropertyType.Boolean,
      description:
        "For tenant limits, e.g. 100 employees MAX or 20 employees/month.",
      required: true,
    },
    {
      name: "isAutogenerated",
      title: "Is Autogenerated",
      type: ModelPropertyType.Boolean,
      description:
        "Generates views and forms at /app/:tenant/:entity-slug, /app/:tenant/:entity-slug/new and /app/:tenant/:entity-slug/:id.",
      required: true,
    },
    {
      name: "hasApi",
      title: "Has API",
      type: ModelPropertyType.Boolean,
      description:
        "Generates API at /api/:entity-slug (GET, POST) and /api/:entity-slug/:id (GET, PUT, DELETE).",
      required: true,
    },
    {
      name: "requiresLinkedAccounts",
      title: "Requires Linked Accounts",
      type: ModelPropertyType.Boolean,
      description: "Rows are shared between 2 tenants.",
      required: true,
    },
    {
      name: "icon",
      title: "Icon",
      type: ModelPropertyType.String,
      description: "Sidebar Icon SVG or image URL.",
      required: true,
    },
    {
      name: "active",
      title: "Active",
      type: ModelPropertyType.Boolean,
      description: "",
      required: true,
    },
    {
      name: "properties",
      title: "Properties",
      customType: {
        name: "Property",
        route: "/docs/core-models/custom-entity/property",
      },
      description:
        "Custom entity Properties, e.g. Full name (Text), Salary (Number), Birthday (Date)...",
      required: false,
    },
    {
      name: "apiKeys",
      title: "API Keys",
      customType: {
        name: "ApiKeyEntity[]",
      },
      description: "CRUD permissions for API Keys",
      required: false,
    },
    {
      name: "webhooks",
      title: "Webhooks",
      customType: {
        name: "EntityWebhook[]",
        route: "/docs/core-models/custom-entity/webhook",
      },
      description:
        "POST, PUT, GET, DELETE webhooks, e.g. for Zapier communication.",
      required: false,
    },
  ]}
/>

## Model

```
model Entity {
  id                     String                       @id @default(cuid())
  createdAt              DateTime                     @default(now())
  updatedAt              DateTime                     @updatedAt
  name                   String                       @unique
  slug                   String                       @unique
  order                  Int                          @unique
  prefix                 String                       @unique
  title                  String // i18n key, eg: models.contract.object
  titlePlural            String //  i18n key, eg: models.contract.plural
  isFeature              Boolean
  isAutogenerated        Boolean
  hasApi                 Boolean
  requiresLinkedAccounts Boolean
  icon                   String
  active                 Boolean
  properties             Property[]
  rows                   Row[]
  apiKeys                ApiKeyEntity[]
  webhooks               EntityWebhook[]
}
```
