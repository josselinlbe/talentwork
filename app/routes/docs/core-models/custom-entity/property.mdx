---
meta:
  title: Property | Custom Entity | Talentwork
  description: Property model life cycle, properties and model
headers:
  Cache-Control: no-cache
---

import DocBreadcrumb from "~/components/ui/docs/DocBreadcrumb";

<DocBreadcrumb
  items={[
    {
      title: "Custom Entity Models",
      routePath: "/docs/core-models/custom-entity",
    },
    {
      title: "Property",
      routePath: "/docs/core-models/custom-entity/property",
    },
  ]}
/>

# Property

- Belongs to an [Entity](entity).
- Multiple types supported: **_NUMBER_**, **_TEXT_**, **_DATE_**, **_ENTITY_**, **_MEDIA_**, and **_SELECT_**.

## Life Cycle

import DocLifeCycle from "~/components/ui/docs/DocLifeCycle";

<DocLifeCycle
  items={[
    {
      route: "/admin/entities/:entity-slug/properties",
      c: true,
      r: true,
      u: true,
      d: true,
    },
  ]}
/>

## Properties

import DocPropertiesTable, {
  ModelPropertyType,
} from "~/components/ui/docs/DocPropertiesTable";

<DocPropertiesTable
  items={[
    {
      name: "entityId",
      title: "Entity",
      customType: {
        name: "Entity",
        route: "/docs/core-models/custom-entity/entity",
      },
      description: "Parent Entity.",
      required: true,
    },
    {
      name: "order",
      title: "Order",
      type: ModelPropertyType.Int,
      description:
        "Order of display at generated views and forms.",
      required: true,
    },
    {
      name: "name",
      title: "Name",
      type: ModelPropertyType.String,
      description: "Internal identifier.",
      required: true,
    },
    {
      name: "title",
      title: "Title",
      type: ModelPropertyType.String,
      description:
        "Can be a i18n key, e.g. First name or models.employee.firstName.",
      required: true,
    },
    {
      name: "type",
      title: "Type",
      customType: {
        name: "PropertyType",
        route: "",
      },
      description:
        "NUMBER, TEXT, DATE, ENTITY, MEDIA, or SELECT",
      required: true,
    },
    {
      name: "isDynamic",
      title: "Is Dynamic",
      type: ModelPropertyType.Boolean,
      description:
        "Is a custom property. Uncheck if you have your own database model (not all property types are supported for this).",
      required: false,
    },
    {
      name: "isDefault",
      title: "Is Default",
      type: ModelPropertyType.Boolean,
      description:
        "There are 3 main default properties on every row: Folio, createdAt, and createdByUser.",
      required: false,
    },
    {
      name: "isHidden",
      title: "Is Hidden",
      type: ModelPropertyType.Boolean,
      description: "Hides at autogenerated forms.",
      required: false,
    },
    {
      name: "isDetail",
      title: "Is Detail",
      type: ModelPropertyType.Boolean,
      description:
        "Is a table detail, e.g. Product name for Invoice lines.",
      required: false,
    },
    {
      name: "pattern",
      title: "Pattern",
      type: ModelPropertyType.String,
      description: "Native input pattern validation.",
      required: false,
    },
    {
      name: "options",
      title: "Options",
      customType: {
        name: "PropertyOption[]",
      },
      description: "For [type]=SELECT properties.",
      required: false,
    },
    {
      name: "values",
      title: "Row Values",
      customType: {
        name: "RowValue[]",
        route: "/docs/core-models/custom-entity/row-value",
      },
      description: "Row property value.",
      required: false,
    },
  ]}
/>

## Model

```
model Property {
  id         String           @id @default(cuid())
  entityId   String
  entity     Entity           @relation(fields: [entityId], references: [id], onDelete: Cascade)
  order      Int
  name       String
  title      String
  type       Int
  parentId   String?
  parent     Property?        @relation(name: "parentProperty", fields: [parentId], references: [id], onDelete: Cascade)
  isDynamic  Boolean
  isDefault  Boolean
  isRequired Boolean
  isHidden   Boolean
  isDetail   Boolean
  pattern    String?
  options    PropertyOption[]
  values     RowValue[]

  @@unique([entityId, order])
  @@unique([entityId, name])
  @@unique([entityId, title])
}
```
