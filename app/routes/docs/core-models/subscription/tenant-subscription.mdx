---
meta:
  title: Tenant Subscription | Subscription | SaasRock
  description: Tenant subscription entity model life cycle, properties and model
headers:
  Cache-Control: no-cache
---

import DocBreadcrumb from "~/components/ui/docs/DocBreadcrumb";

<DocBreadcrumb
  items={[
    {
      title: "Subscription Models",
      routePath: "/docs/core-models/subscription",
    },
    {
      title: "Tenant Subscription",
      routePath:
        "/docs/core-models/subscription/tenant-subscription",
    },
  ]}
/>

# Tenant Subscription

- Belongs to a [Tenant](/docs/core-models/account/tenant).
- Determines the current Tenant subscription and its limits.

## Life Cycle

import DocLifeCycle from "~/components/ui/docs/DocLifeCycle";

<DocLifeCycle
  items={[
    {
      route: "/app/:tenant/settings/subscription",
      c: true,
      r: true,
      u: true,
      d: true,
    },
    {
      route: "/admin/accounts/:id",
      c: true,
      r: true,
      u: true,
      d: false,
    },
  ]}
/>

## Properties

import DocPropertiesTable, {
  ModelPropertyType,
} from "~/components/ui/docs/DocPropertiesTable";

<DocPropertiesTable
  items={[
    {
      name: "tenantId",
      title: "Tenant",
      customType: {
        name: "Tenant",
        route: "/docs/core-models/account/tenant",
      },
      description: "Parent Tenant",
      required: true,
    },
    {
      name: "stripeCustomerId",
      title: "Stripe Customer ID",
      type: ModelPropertyType.String,
      description: "",
      required: false,
    },
    {
      name: "stripeSubscriptionId",
      title: "Stripe Subscription ID",
      type: ModelPropertyType.String,
      description: "",
      required: false,
    },
    {
      name: "subscriptionPriceId",
      title: "Subscription Price",
      customType: {
        name: "SubscriptionPrice",
        route: "/docs/core-models/subscription/price",
      },
      description:
        "Selected subscription price, e.g. Monthly Price (of the Basic Subscription Product).",
      required: false,
    },
    {
      name: "quantity",
      title: "Quantity",
      type: ModelPropertyType.Int,
      description:
        "For products.model=[PER_SEAT] seats quantity input",
      required: false,
    },
  ]}
/>

## Model

```
model TenantSubscription {
  tenantId             String             @unique
  tenant               Tenant             @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  stripeCustomerId     String?
  stripeSubscriptionId String?
  subscriptionPriceId  String?
  subscriptionPrice    SubscriptionPrice? @relation(fields: [subscriptionPriceId], references: [id])
  quantity             Int
}
```
