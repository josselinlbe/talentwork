---
meta:
  title: Linked Account | Account | SaasRock
  description: SaasRock Linked Account Core Model - Linked account Life cycle, Properties and Model.
headers:
  Cache-Control: no-cache
---

import DocBreadcrumb from "~/components/ui/docs/DocBreadcrumb";

<DocBreadcrumb
  items={[
    {
      title: "Account Models",
      routePath: "/docs/core-models/account",
    },
    {
      title: "Linked Account",
      routePath: "/docs/core-models/account/linked-account",
    },
  ]}
/>

# Linked Account

- Defines a relationship between 2 tenants so they could share resources, such as [Custom Entity Rows](/docs/core-models/custom-entity/row).
- Has 1 [Tenant](/docs/core-models/account/tenant) **provider** and 1 [Tenant](/docs/core-models/account/tenant) **client**.

## Life Cycle

import DocLifeCycle from "~/components/ui/docs/DocLifeCycle";

<DocLifeCycle
  items={[
    {
      route: "/app/:tenant/settings/linked-accounts",
      c: true,
      r: true,
      u: false,
      d: true,
    },
  ]}
/>

## Properties

import DocPropertiesTable, {
  ModelPropertyType,
} from "~/components/ui/docs/DocPropertiesTable";

<DocPropertiesTable
  items={[
    {
      name: "providerTenantId",
      title: "Tenant Provider",
      customType: {
        name: "Tenant",
        route: "/docs/core-models/account/tenant",
      },
      description: "",
      required: true,
    },
    {
      name: "clientTenantId",
      title: "Tenant Client",
      customType: {
        name: "Tenant",
        route: "/docs/core-models/account/tenant",
      },
      description: "",
      required: true,
    },
    {
      name: "status",
      title: "Status",
      customType: {
        name: "LinkedAccountStatus",
      },
      description: "PENDING, LINKED, or REJECTED",
      required: true,
    },
  ]}
/>

## Model

```
model LinkedAccount {
  id                String   @id @default(cuid())
  createdAt         DateTime @default(now())
  createdByUserId   String
  createdByUser     User     @relation(fields: [createdByUserId], references: [id], onDelete: Cascade)
  createdByTenantId String
  createdByTenant   Tenant   @relation("createdByTenant", fields: [createdByTenantId], references: [id], onDelete: Cascade)
  providerTenantId  String
  providerTenant    Tenant   @relation("providerTenant", fields: [providerTenantId], references: [id], onDelete: Cascade)
  clientTenantId    String
  clientTenant      Tenant   @relation("clientTenant", fields: [clientTenantId], references: [id], onDelete: Cascade)
  status            Int
  rows              Row[]
}
```
