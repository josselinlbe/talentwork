import { Visibility } from "./dtos/shared/Visibility";

const DEFAULT_PAGE_SIZE = 10;
const DEFAULT_ROW_VISIBILITY = Visibility.Tenant;

export default {
  DEFAULT_PAGE_SIZE,
  DEFAULT_ROW_VISIBILITY,
};
